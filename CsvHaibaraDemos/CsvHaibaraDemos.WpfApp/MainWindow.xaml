<Window x:Class="CsvHaibaraDemos.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CsvHaibaraDemos.WpfApp"
        mc:Ignorable="d"
        Height="300" Width="700" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Width" Value="80" />
            <Setter Property="Height" Value="40" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style TargetType="{x:Type ListBox}">
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="btnSelectFile" Margin="10,0,0,0" Content="Select file:" Click="btnSelectFile_Click" />
            <TextBox x:Name="tbxFilePath" Margin="30,0,0,0" Width="560" Height="60" IsEnabled="False" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,50,0,50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="btnRead" Content="Read" IsEnabled="False" Click="btnRead_Click" />
            <Button Grid.Column="1" x:Name="btnCancel" Content="Cancel" IsEnabled="False" Click="btnCancel_Click" />
        </Grid>

        <TextBlock Grid.Row="2" x:Name="tblCount" Margin="20,0,0,0" />
    </Grid>
</Window>
